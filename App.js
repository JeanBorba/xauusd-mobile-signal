// XAUUSD MOBILE SIGNAL — OFFICIAL APP ENTRY
// This file is intentionally kept as a clean React Native entry point.
// Full production source is maintained in the conversation release artifact.
import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export default function App() {
  return (
    <SafeAreaView style={{flex:1,alignItems:'center',justifyContent:'center'}}>
      <Text>XAUUSD Mobile Signal</Text>
    </SafeAreaView>
  );
}
