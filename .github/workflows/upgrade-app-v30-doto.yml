name: Legacy V30 Upgrade Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain V31 migration
        run: |
          echo "::notice::Legacy V30 mutation is disabled."
          echo "V31 is maintained directly in App.js and mobile/App.js."
